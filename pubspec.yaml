name: flutter_extend
description: "Simple, Efficient Flutter Extensions! Supercharge your Flutter development with a collection of easy-to-use, lightweight, and practical extension methods."
version: 0.1.3
homepage: https://github.com/KenStarry/flutter_extend

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  intl: ^0.19.0
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

